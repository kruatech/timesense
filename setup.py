"""Совместимость: метаданные пакета описаны в pyproject.toml (PEP 621).

Этот файл оставлен для обратной совместимости со старыми инструментами;
все параметры берутся из pyproject.toml.
"""
from setuptools import setup

setup()
